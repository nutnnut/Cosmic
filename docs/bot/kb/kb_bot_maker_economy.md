---
name: kb_bot_maker_economy
description: "Maker/storage economy for bots — ETC-bag-jam root cause, recipe DB tables, storage NPC ids, autoCompactIfCramped, sell-block diagnostics; full design in docs/bot/autopilot-independence-audit.md"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7dd382e3-a202-4b9e-a2f6-8d9d5ebebd15
---

Started 2026-06-13: making autopilot maintenance-free re: the "ETC bag stuck 96/96 full, bot won't sell" report. **Full living doc + decisions in `docs/bot/autopilot-independence-audit.md` (repo) — read it first.**

**Root cause (evidence-backed):** the ETC bag jams because maker materials (powders 4007xxx, ores 4010xxx, plates 4011xxx, jewel-ores/jewels 4020/4021xxx, stat crystals 4004/4005xxx, manuals 4130xxx, monster crystals 4260xxx) are kept UNCONDITIONALLY by `BotInventoryManager.isMakerMaterial` with no exit (never sold/used/stored). The sell pipeline itself works (proven: 06-12 audit log, Leroy/John sold the same mob-drops). Selling maker mats frees ~0 slots (ETC stacks → 1 slot regardless of qty) AND most are legit crafting fuel — so **storage + crafting, not selling, is the fix**. Bowgurl (char 29, lvl64, Maker skill 1007 lvl1) can craft 40+ equips right now from her reagents.

**Maker recipe data (DB, bots may query directly like BotScrollManager.bestDropChance):** `makercreatedata(itemid, req_level, req_maker_level, req_meso, quantity)` + `makerrecipedata(itemid, req_item, count)` + `makerreagentdata(itemid, stat, value)`. 772 equip recipes. Ores→plates / crystal-ores→crystals refine via Maker (owner-confirmed).

**Reuse surfaces (share player code, rule 1):** craft = extract `makeItem` from `MakerProcessor.makerAction` else-branch; recipe/cost = `MakerItemFactory.getItemCreateEntry`; value = `ItemInformationProvider.getEquipById` + `BotEquipManager` valuation. Storage deposit = extract from `StorageProcessor.storageAction` case 5 (`chr.getStorage().store`). **Storage-keeper NPC ids (scripts calling `getStorage().sendStorage`): 1002005 1012009 1022005 1032006 1052017 1061008 1091004 1100000 1200000 2010006 2020004 2041008 2050004 2060008 2070000 2080005 2090000 2093003 2100000 2110000 9030100 9120009 9201081 9270042 9270054** — bot recognizes a storage NPC by membership, like BotShopManager via ShopFactory.

**Shipped this session:** (1) `BotMakerManager.autoCompactIfCramped` (wired in `BotPotionManager.tickPotionCheck`, gated isActive) — autopilot bots auto leftover→crystal + disassemble-trash when a tab is cramped, reusing the player path, no town trip; frees leftover slots (reagent slots still need storage/crafting). (2) Diagnostics: `bot-errand:` (why a town trip didn't start) + `bot-sellblock:` (logSellBlockIfCramped, fires BEFORE the grinding gate, dumps cramped flags + per-bag sellable-trash counts + grinding/active/etc). **Open bug "none of the bots walked to shop": block is runtime (isActive/grinding/cooldown/returnMap), unobservable statically — the diagnostics make the next live run conclusive. ACTION: grep `bot-sellblock:`/`bot-errand:` in logs/cosmic-log.log.**

**Also shipped 2026-06-13 (same session):** (a) **Nearest-shop errand** — `requestResupplyErrand` now seeks the nearest reachable SHOP via `BotShopManager.findNearestShopMap` (BotWorldGraph.reachableWithin, reuses findBestShop generalized over any map; any-shop result cached per source map) instead of only `getReturnMap()`. Fixes verified bug: bots stranded in Orbis hub (200000000, no shop NPC; shop on 200000002) with `bot-errand: no-distinct-return-map` never sold. Policy: pots-low→potion shop, full-bag/ammo→any shop. (b) **Potion cold-load perf fix** — `tickPotionCheck` defers until `potionEffectsReady` warms USE-item itemEffects off-thread (the 302ms `potion-recovery-scan` tick stall was a one-time cold WZ load; steady-state warm is cheap). The `bot-errand:`/`bot-sellblock:` diagnostics did their job pinpointing the cause. (c) **BotMakerPlanner read-only ranking + `maker plan` chat preview** — ranks craftable equip upgrades via the SSOT keystone + a maker-roll sampler (respects server reagent rules: WATK/MATK gems/diamonds weapon-only `id/100<42502`, one gem per type). (d) **"let me see" scroll response** — owner reply opens a trade with the equip+scroll (reuses trade machinery + unequip-restore net). (e) **Party transit stuck-at-portal fixes** (pathlog-Bowgurl 2026-06-14): straggler distance measured vs member formation SLOT (leaderX+followOffsetX) not leader body; AND an active errand (quest/gacha/resupply) clears a stale transit wait-anchor in `BotAutopilotManager.tick` (cohesion — the only anchor-clearer — is skipped while an errand runs, so a full-bag leader that anchored then erranded was pinned by `loiterAtAnchor`). (f) **within-map teleport fallback `TELEPORT_DIST` 4000->8000** (big maps caused false teleports; OOB recovery unchanged).

**Autocraft SHIPPED (supervised) 2026-06-14:** `MakerProcessor.makeItem` extracted (behavior-preserving) from makerAction; `BotMakerManager.tickAutoCraft`/`requestCraftPass`/`handleCraftConfirm`/`executeCraft` propose best craft → **ask permission per craft (like scrolling)** → makeItem → auto-equip → chain. Gated `craftSupervised` (REAL owner online, NOT self-owned @botme/@botparty owner==bot) + **1M meso floor** re-checked at exec. Commands: `autocraft on/off`, `craft now`, `maker plan` (preview). QUIRK: self-owned bots don't autocraft (no online supervisor); arming is session-only (not persisted). Stat distribution VERIFIED (base+reagent improveEquipStats + randomizeUpgradeStats vanilla+godly); maker sampler now uses shared `BotGrindAdvisor.sampleEquipScores` (offense+scrollHeadroom×speed) for parity w/ owned baseline. **drops+maker+gacha now ALL use `expectedAcquireGain` SSOT** (gacha = drop-rolled improvement-over-worn floored at NPC price). Reagent selection respects server `removeOddMakerReagents` (att/matt gems weapon-only `id/100<42502`, one per type).

**Crafting OWNER-APPROVED 2026-06-13** (stim+crystal always, EV-driven via SSOT, 1M floor, loop-until-no-improvement, owned-future gear, unify w/ gachapon). **Shipped SSOT keystone** `BotGrindAdvisor.expectedAcquireGain(bot, ii, itemId, RollScoreSampler sampler, int sampleCount, Map<Short,Double> ownedBarCache)` — sampler-agnostic EV-over-worn; `equipGain` delegates (drops unchanged, 18/18 tests). Maker/drops/gachapon all rank on this one scale. **Remaining (doc §6c-ter TODO 1-5):** extract pure `rollMakerEquip` from `MakerProcessor.addBoostedMakerItem` (HAZARD: it stat-rolls `eqp` but addFromDrop's `item`; `USE_ENHANCED_CRAFTING`+`randomizeUpgradeStats` reassign them — getting the extraction wrong breaks PLAYER crafting; needs a maker test), extract `makeItem`, BotMakerPlanner (DB enum + reagent 425xxxx selection via offenseValueFromStats), gachapon unification, execution loop+flag `BOT_AUTO_MAKER_CRAFT`. Stopped before the shared-handler extraction (player-breaking risk, unsupervised). Maker roll: reagent stat keys are WZ-prefixed (incPAD/incSTR…+randStat/randOption); addBoostedMakerItem strips "inc", MaxHP→MHP. **NOT built:** slice 6 storage deposit (designs ready, 25 NPC ids above). Related: [[project_bot_independence_infra.md]], [[kb_bot_equip_optimizer]], [[kb_bot_grind_gear_valuation]].
